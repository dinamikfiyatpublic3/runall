name: Test Run

on:
  workflow_dispatch:  # Manuel ve API üzerinden tetiklenebilir

jobs:
  run-tests:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Run test script
        run: |
          echo "🧪 Running tests..."
          # Buraya test komutlarını ekle (örneğin pytest, jest, vs.)
          echo "✅ Test run complete!"
